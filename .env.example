# Credentials per project/service. Prefix: <CODE>_<SERVICE>_
# Base URLs are in each project's config.json.

# komp/frontend-canvas — Canvas LMS login credentials (chromium only)
TEST_KOMP_CHROMIUM_USERNAME=
TEST_KOMP_CHROMIUM_PASSWORD=
